<h1> <%= @artist.name%> </h1>

<% @genres.each do |genre| %>
  <a href=<%= "/genres/#{genre.slug}"%>> <%= genre.name %></a> <br>
<% end %>

<% @songs.each do |song| %>
  <a href=<%= "/songs/#{song.slug}"%>> <%= song.name %></a> <br>
<% end %>